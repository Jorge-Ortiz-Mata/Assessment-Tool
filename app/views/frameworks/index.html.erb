<%= turbo_stream_from "frameworks" %>
<div class="w-full flex inline-flex flex-col text-center mt-16 pb-2">
  <h1 class="font-bold text-3xl pb-10">Frameworks.</h1>
  <p>
    Frameworks have criteria. You can create several frameworks with a unique name. You can also set
    a description about the framework that you created.  
  </p>
</div>

<%= turbo_frame_tag 'frameworks_form' do %>
  <div class="text-center pt-10">
    <%= link_to 'Create Framework', new_framework_path,
      class: "bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded" %>
  </div>
<% end %>

<div class="flex flex-col" style="visibility: visible;">
  <table class="text-center m-20 border-2 border-gray-700">
    <thead class="border-b bg-backcolor">
      <tr>
        <th scope="col" class="text-sm font-medium text-white px-6 py-4">
          Name
        </th>
        <th scope="col" class="text-sm font-medium text-white px-6 py-4">
          Description
        </th>
        <th scope="col" class="text-sm font-medium text-white px-6 py-4">
          Criteria
        </th>
        <th scope="col" class="text-sm font-medium text-white px-6 py-4">
          Created at
        </th>
        <th scope="col" class="text-sm font-medium text-white px-6 py-4">
          Options
        </th>
      </tr>
    </thead class="border-b">
    <tbody id="frameworks">
      <% @frameworks.each do |framework| %>
          <%= render framework %>
      <% end %>
      <%= turbo_frame_tag "all-frameworks" do %>
      <% end %>
    </tbody>
  </table>
</div>
